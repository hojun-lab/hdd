# Week 2 Mission

> 제출 기한: Week 3 시작 전
> 제출 방법: 이 파일의 "답변" 섹션에 직접 작성

---

## Mission 1: Config Defense

### 시나리오

Spring Boot 애플리케이션을 운영 중이다. 다음 조건이 주어졌다:

| 항목 | 값 |
|------|-----|
| 동시 사용자 | 500명 |
| API 평균 응답시간 | 200ms |
| 쿼리 평균 실행시간 | 30ms |
| 요청당 쿼리 수 | 3회 |
| DB 서버 | 8코어, SSD |
| 애플리케이션 서버 | 2대 |

### 과제

아래 5개 파라미터에 대해 **값 + 근거**를 작성하라. "10으로 설정"이 아니라 "10으로 설정한 이유는 ..."이 핵심이다.

| 파라미터 | 값 | 근거 |
|---------|-----|------|
| `maximumPoolSize` | | |
| `minimumIdle` | | |
| `connectionTimeout` | | |
| `maxLifetime` | | |
| `leakDetectionThreshold` | | |

### 추가 질문 (반드시 답변)

1. 애플리케이션 서버가 2대인데, DB 서버의 `max_connections`는 최소 몇으로 설정해야 하는가? 계산 과정을 보여라.
2. 트래픽이 평소의 3배로 폭증했다. `maximumPoolSize`를 올리는 게 답인가? 아니라면 어떻게 대응하겠는가?
3. `leakDetectionThreshold`를 1000ms(1초)로 설정하면 어떤 문제가 발생하겠는가?

---

## Mission 2: Benchmark Report — v1 vs v2

### 과제

MiniPool v1(synchronized)과 MiniPool v2(ThreadLocal + CAS + Semaphore)를 **동일 조건**에서 비교하라.

### 테스트 조건

| 항목 | 값 |
|------|-----|
| 풀 사이즈 | 10 |
| iteration | 1000 |
| connectionTimeout | 30000ms |
| 쿼리 | SELECT 1 |

스레드 수를 변경하며 3회 측정:

| 스레드 | v1 처리량 | v1 실패 | v2 처리량 | v2 실패 |
|--------|----------|---------|----------|---------|
| 10 | | | | |
| 50 | | | | |
| 200 | | | | |

### 분석 (반드시 작성)

1. **v1과 v2의 처리량 차이가 어디서 발생하는가?** 스레드 수에 따라 차이가 커지는가 작아지는가?
2. **실패율 차이의 원인**은 무엇인가? v1에는 없고 v2에는 있는 것이 뭔가?
3. **v2가 v1보다 무조건 좋은가?** v2의 단점은 없는가?
4. **프로덕션에서 어떤 버전을 선택하겠는가?** 이유는?

---

## 제출 형식

이 파일에 직접 답변을 채우거나, 별도 파일로 작성 후 경로를 여기에 기록하라.

```
답변 파일: ________________
제출일: ________________
```
